version: '3.8'

services:
  youtube-etl:
    build: .
    container_name: youtube_etl_dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/youtube_data.db
    restart: unless-stopped
