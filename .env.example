# YouTube API Key (Optional. If empty, the pipeline will use mock data)
YOUTUBE_API_KEY=

# Search query for extracting videos
SEARCH_QUERY="data engineering"

# Maximum number of videos to fetch per run (Max 50 for search API)
MAX_RESULTS=50

# Database Path
DB_PATH="data/youtube_data.db"
